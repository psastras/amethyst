<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    {% if title %}
      <title>{{ site.data.meta.site_name }} - {{ title }}</title>
    {% else %}
      <title>{{ site.data.meta.site_name }}</title>
    {% endif %}
    <link rel="stylesheet" href="{{ site.data.meta.base_path }}/_styles/pure-min.css" />
    <link rel="stylesheet" href="{{ site.data.meta.base_path }}/_styles/grids-responsive-min.css" />
    <link rel="stylesheet" href="{{ site.data.meta.base_path }}/_styles/default.css" />
  </head>
  <body>
  <div id="layout" class="pure-g">
    <div class="sidebar pure-u-1 pure-u-md-1-4">
      {% include '_layouts/components/nav.liquid' %}
    </div>
    <main class="content pure-u-1 pure-u-md-3-4">
